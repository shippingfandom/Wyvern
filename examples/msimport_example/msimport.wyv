msimport "math.src" {
    class Math {
        public fn Pi() -> number;
        public fn Factorial(n: number) -> number;

        public static fn New() -> Math;
    }
}

const math = Math.New();
print([math.Pi(), math.Factorial(5)]);
